#ifndef COMMON_H
#define COMMON_H

#include <QDebug>
enum class logStatus{log, warning, error};
enum class dialogKinds{stander, question, warning, error};
//{HKEY_CLASSES_ROOT, HKEY_CURRENT_USER, HKEY_LOCAL_MACHINE, HKEY_USERS, HKEY_CURRENT_CONFIG};
#define VERSION 0.1

#define pathEmpty "Path must not be empty"

#endif // COMMON_H
